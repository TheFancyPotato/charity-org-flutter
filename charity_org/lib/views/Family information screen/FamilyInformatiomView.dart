import 'package:flutter/material.dart';

class FamilyInformationView extends StatelessWidget {
  const FamilyInformationView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("family view")),
    );
  }
}
